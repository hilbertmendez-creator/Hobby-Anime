# Pinned runtime dependency versions for reproducible NAS rebuilds.
# Regenerate after intentional upgrades with:
#   pip install . && pip freeze > constraints.txt   (then prune dev-only lines)
# The Dockerfile installs the project with `-c constraints.txt` so an
# unattended `docker compose up -d --build` cannot pull a breaking release.
APScheduler==3.11.3
feedparser==6.0.12
qbittorrent-api==2026.7.0
requests==2.34.2
fastapi==0.139.2
uvicorn==0.51.0
# Transitive dependencies
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
certifi==2026.6.17
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
sgmllib3k==1.0.0
sortedcontainers==2.4.0
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
tzlocal==5.4.4
urllib3==2.7.0
watchfiles==1.2.0
websockets==16.1.1
# Dev/test-only (used by starlette TestClient; not installed in the container runtime)
httpx2==2.7.0
httpcore2==2.7.0
truststore==0.10.4
